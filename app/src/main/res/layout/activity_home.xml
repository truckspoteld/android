<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">



    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_main"
        app:menu="@menu/activity_main_drawer" />

    <me.ibrahimsn.lib.SmoothBottomBar
        android:id="@+id/bottomNav"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:elevation="0dp"
        app:backgroundColor="@color/nav_item_selector"
        app:indicatorColor="#BEDBFA"
        app:indicatorRadius="50dp"
        app:textColor="@color/login_btn_gradient_start"
        app:textSize="10sp"
        app:iconSize="20dp"
        app:itemPadding="10dp"
        app:iconTint="@color/nav_item_text_color"
        app:iconTintActive="@color/login_btn_gradient_start"
        app:activeItem="0"
        app:menu="@menu/test" />


</androidx.drawerlayout.widget.DrawerLayout>